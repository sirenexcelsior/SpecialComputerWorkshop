# GeometrySage

```python
    sage: load("geometry.sage")  # Gergon.sage, Lemoine.sage, Nagel.sage
    sage: a = vector([0, 0])
    sage: b = vector([5, 1])
    sage: c = vector([3, 5])
    sage: drawTriangle(a, b, c)
```